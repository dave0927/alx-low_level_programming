#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: array of pointers to argument
 * Return: 0
 */

int main(int argc, char *argv[])

{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
