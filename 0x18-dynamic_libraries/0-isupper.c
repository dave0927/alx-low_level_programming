#include "main.h"

/**
* _isupper - checks for uppercase characters
* @c: the character to check
* Description: chack if a given character is uppercase or not
* Return: 1 if `c` is uppercase or returns 0
*/

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
