#include "main.h"

/**
* add - sum two intigers and return the result
*
* @a: the first number
* @b: the second number
*
* Return: returns the sum of `a` and `b`
*/

int add(int a, int b)
{
	return (a + b);
}
